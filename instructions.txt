#run locally 
npm run dev

# Optional (recommended) env vars for blogs/SEO/integrations
# Create a file named `.env.local` in the project root:
#
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_DISQUS_SHORTNAME=your-shortname

# Contact form → Google Sheets (via Apps Script webhook)
# CONTACT_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/<DEPLOYMENT_ID>/exec
# CONTACT_SHEETS_SECRET=<long-random-string>

